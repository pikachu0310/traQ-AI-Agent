# Internal Demo Summary

This file is a local fixture used by the Mastra MCP tools in the MVP repository.

Key points:
- The MVP prioritizes end-to-end behavior over architecture complexity.
- Session mapping is persisted by conversation key, never by directly reusing thread id.
- Codex is executed in non-interactive mode with JSON streaming.
- The Mastra process is used only as an MCP tool provider.
